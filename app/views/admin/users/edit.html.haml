- content_for :page_title, "Admin panel / Users"
- content_for :headline, "Admin panel / Users"

%h2= @user.name

= form_for [:admin, @user] do |f|
  = f.label :expiry_date
  = f.text_field :expiry_date, class: "calendar"
  = f.submit "Update", class: "blue"